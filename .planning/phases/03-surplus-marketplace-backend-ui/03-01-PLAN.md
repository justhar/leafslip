---
phase: 03-surplus-marketplace-backend-ui
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - app/actions/surplus.ts
  - app/dashboard/layout.tsx
  - app/components/Sidebar.tsx
  - app/dashboard/surplus/page.tsx
  - app/components/SurplusDashboard.tsx
autonomous: true
requirements:
  - MARKET-02
  - MARKET-03
must_haves:
  truths:
    - Surplus listings deduct stock from main products if linked
    - Surplus dashboard displays active, reserved, and expired listings
    - AI generates surplus item descriptions
  artifacts:
    - path: "app/actions/surplus.ts"
      provides: "Server actions for managing surplus and reservations"
      contains: "createSurplusListing"
    - path: "app/dashboard/surplus/page.tsx"
      provides: "Surplus management view for MSMEs"
      contains: "SurplusDashboard"
---

<objective>
Implement the Surplus Marketplace backend logic and MSME dashboard interface.

**Purpose:**
- Provide MSMEs a place to post excess inventory at a discount.
- Implement server actions for listing CRUD, stock deduction, and AI description generation.
- Build the UI to manage and view these listings.
</objective>

<execution_context>
@~/.gemini/antigravity/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
From 03-CONTEXT.md and 03-UI-SPEC.md:
- Surplus tab added to dashboard.
- Stock is deducted automatically when linking a surplus listing to a product.
- AI is used to generate descriptions.
- Reservations use a 6-character pickup code.
</context>

<tasks>
<task type="auto">
  <name>Task 1: Surplus Server Actions</name>
  <files>
    app/actions/surplus.ts
  </files>
  <action>
    Create a new server actions file to handle the surplus backend:
    - `getSurplusListings()`: fetches MSME's listings
    - `createSurplusListing()`: inserts into `surplus_listings`, deducts `products.stock` if linked.
    - `generateSurplusDescription()`: takes a title and generates a compelling 2-sentence description using Gemini (`generateText`).
    - `completeReservation()`: verifies 6-character code and marks `surplus_reservations` as picked up.
  </action>
  <verify>
    <automated>grep -c "createSurplusListing" app/actions/surplus.ts</automated>
  </verify>
  <done>
    Backend logic for MSME surplus management is complete.
  </done>
</task>

<task type="auto">
  <name>Task 2: Add Surplus Navigation</name>
  <files>
    app/components/Sidebar.tsx
  </files>
  <action>
    Update the sidebar navigation to include a link to `/dashboard/surplus` with a suitable icon (e.g., `Store` or `Tag`).
  </action>
  <verify>
    <automated>grep -c "/dashboard/surplus" app/components/Sidebar.tsx</automated>
  </verify>
  <done>
    Surplus navigation link added.
  </done>
</task>

<task type="auto">
  <name>Task 3: Build Surplus Dashboard UI</name>
  <files>
    app/dashboard/surplus/page.tsx
    app/components/SurplusDashboard.tsx
  </files>
  <action>
    Implement the Surplus dashboard view:
    - Displays stats and active listings in a table or grid.
    - Modal/Form to create a new listing (includes AI description generation).
    - Modal requires user to confirm if it deducts from main product stock.
    - Status badges for the listings (Active, Reserved, Expired).
  </action>
  <verify>
    <automated>grep -c "SurplusDashboard" app/dashboard/surplus/page.tsx</automated>
  </verify>
  <done>
    MSME user interface for managing surplus listings is functional.
  </done>
</task>
</tasks>
