---
phase: 04-guest-marketplace-appointments-ui
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - app/actions/market.ts
  - app/market/page.tsx
  - app/components/Marketplace.tsx
autonomous: true
requirements:
  - MARKET-04
  - MARKET-05
must_haves:
  truths:
    - Guests can browse active surplus listings without logging in
    - Guests can reserve an item and receive a 6-character code
    - Reserving an item correctly deducts the remaining quantity
  artifacts:
    - path: "app/actions/market.ts"
      provides: "Server actions for public marketplace"
      contains: "createReservation"
    - path: "app/market/page.tsx"
      provides: "Public marketplace page"
      contains: "Marketplace"
---

<objective>
Implement the public Guest Marketplace for buyers to browse and reserve surplus listings.

**Purpose:**
- Provide a public-facing storefront.
- Enable frictionless guest checkout and reservation flow.
</objective>

<execution_context>
@~/.gemini/antigravity/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
From 04-CONTEXT.md and 04-UI-SPEC.md:
- `/market` is public.
- Users see item cards, can search by title.
- Reserving requires Name, Phone, Qty, Pickup note.
- Confirmation yields a 6-character code.
</context>

<tasks>
<task type="auto">
  <name>Task 1: Market Server Actions</name>
  <files>
    app/actions/market.ts
  </files>
  <action>
    Create public-facing server actions:
    - `getActiveListings()`: fetch all surplus listings where status is 'active', `remainingQuantity > 0`, and `expiresAt` is in the future.
    - `createReservation(data)`: creates a reservation, generates a random 6-character alphanumeric code, deducts `remainingQuantity` on the listing. If `remainingQuantity` becomes 0, marks listing status as 'reserved' or 'sold_out'.
  </action>
  <verify>
    <automated>grep -c "createReservation" app/actions/market.ts</automated>
  </verify>
  <done>
    Backend logic for public marketplace reservations is complete.
  </done>
</task>

<task type="auto">
  <name>Task 2: Build Marketplace UI</name>
  <files>
    app/market/page.tsx
    app/components/Marketplace.tsx
  </files>
  <action>
    Implement the public marketplace page:
    - A clean hero section and search bar.
    - A grid of items showing stock and expiry.
    - A modal to enter reservation details (Guest checkout).
    - Success view displaying the 6-character code prominently.
  </action>
  <verify>
    <automated>grep -c "Marketplace" app/market/page.tsx</automated>
  </verify>
  <done>
    Public marketplace UI is fully functional and responsive.
  </done>
</task>
</tasks>
