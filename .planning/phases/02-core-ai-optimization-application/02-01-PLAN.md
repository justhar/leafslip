---
phase: 02-core-ai-optimization-application
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - app/actions/dashboard.ts
  - app/actions/products.ts
  - app/actions/receipts.ts
autonomous: true
requirements:
  - OPT-02
  - OPT-03
  - OPT-04
must_haves:
  truths:
    - Structured outputs are enforced via Zod schema and generateObject
    - Duplicate identical AI calls hit a cache instead of the API
    - Background AI tasks run asynchronously without blocking synchronous actions
  artifacts:
    - path: "app/actions/dashboard.ts"
      provides: "Dashboard with optimized AI calls and async behavior"
      contains: "unstable_after"
    - path: "app/actions/products.ts"
      provides: "Product insights using DB cache"
      contains: "generateObject"
---

<objective>
Refactor AI calls to use async background execution, verify structured outputs, and ensure caching strategies are implemented according to the Phase 2 context.

**Purpose:**
- Use `unstable_after` for pre-computing insights or background jobs to prevent blocking the main thread.
- Ensure `products.ts` and `receipts.ts` properly use DB/in-memory caching.
- Verify structured output integration.

</objective>

<execution_context>
@~/.gemini/antigravity/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
From 02-CONTEXT.md:
- Structured outputs via Zod/generateObject (already in products.ts/receipts.ts).
- Async background tasks via `unstable_after` where possible to keep the UX snappy.
</context>

<tasks>
<task type="auto">
  <name>Task 1: Verify and Ensure Caching Strategy</name>
  <files>
    app/actions/products.ts
    app/actions/receipts.ts
  </files>
  <action>
    - Verify `products.ts` uses DB caching for `generateObject` insights (already implemented).
    - Ensure `receipts.ts` uses an in-memory cache for duplicate OCR requests within a 24h period (already implemented).
  </action>
  <verify>
    <automated>grep -c "aiInsights" app/actions/products.ts</automated>
    <automated>grep -c "receiptAiCache" app/actions/receipts.ts</automated>
  </verify>
  <done>
    Caching strategies are fully implemented across product insights and OCR.
  </done>
</task>

<task type="auto">
  <name>Task 2: Implement Async Background Pre-computation</name>
  <files>
    app/actions/receipts.ts
    app/actions/dashboard.ts
  </files>
  <action>
    Modify `createReceipt` in `receipts.ts` to use `unstable_after` (from `next/server`) to pre-warm the dashboard insights in the background. When a new receipt is added, it triggers the dashboard insights generation silently so that the next time the user loads the dashboard, the cache is fresh or pre-warmed.
    Wait, `dashboard.ts` currently doesn't cache its insights. We need to implement a simple DB cache for dashboard insights so `unstable_after` can populate it.
  </action>
  <verify>
    <automated>grep -c "unstable_after" app/actions/receipts.ts</automated>
  </verify>
  <done>
    `unstable_after` is integrated into `createReceipt` to prevent blocking the main thread while updating AI insights.
  </done>
</task>
</tasks>
